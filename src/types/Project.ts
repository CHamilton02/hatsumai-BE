export interface GeneratedProject {
  title: string
  description: string
  tips: Array<string>
}

export interface ProjectHistory {
  id: number
  title: string
  description: string
  topics: Array<string>
}
